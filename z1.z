(fun (write buf len)
  (return (syscall 1 1 buf len)))

(fun (strlen str)
  (var l 0)
again
  (goto done (not (b@ str l)))
  (= l (+ l 1))
  (goto again)
done
  (return l))

(fun (main)
  (write hello (strlen hello)))

(data hello (b 'Hello World' 10 0))
