@cat 1 Programming languages
@cat 0 Z1
@cat 10 Runtime
@cat 0 Dynamic context

@** The global context

Zollern books can only contain read-only code and data. If a book
needs some global mutable state, it can use a field...

@=
(struct ctxt/contexts
  disk
  books
  comp
  mem
  fs
  gfx
  app
  ui
  tx)
